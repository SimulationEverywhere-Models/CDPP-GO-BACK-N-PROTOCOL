gbn.bat  and gbn2.bat is used for simulation of the TOP model with two different event function.

Other batch filed in thsi projects are used for simulation of individual components, either atomic or coupled.

